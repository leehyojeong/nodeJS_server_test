// 화살표 함수
// 변수에 대입하면 나중에 재사용 가능
function add1(x, y){
    return x+y;
}

const add2 = (x,y)=>{
    return x+y
}

const add3 = (x,y)=>x+y

const add4 = (x,y)=>(x+y)

function not1(x){
    return !x
}

const not2 = x => !x